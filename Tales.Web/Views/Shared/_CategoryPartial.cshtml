@model IEnumerable<Tales.Model.Category>

<header>
    <h3>Categories</h3>
</header>
<div class="body">
    <ul class="tales-list">
        @foreach (var item in Model)
        {
            <li><a href="#">@item.Name</a></li>
        }
        
    </ul>
</div>
