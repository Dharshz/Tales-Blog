@model IEnumerable<Tales.Model.Tag>

<header>
    <h3>Tags</h3>
</header>
<div class="body clearfix">
    <ul class="tags">
        @foreach (var item in Model)
        {
            <li><a href="#">@item.Name</a></li>
        }
    </ul>
</div>
